PIXI.settings.FAIL_IF_MAJOR_PERFORMANCE_CAVEAT = !1;
// const IMG_WIDTH = window.screen.width - (window.screen.width * .2),
//     IMG_HEIGHT = window.screen.width / 3,
//     SCALE_FACTOR = 20,
//     app = new PIXI.Application({ width: IMG_WIDTH, height: IMG_HEIGHT}),
//     container = document.getElementById("parallax");
// // container.display = "fixed",
// container.appendChild(app.view);
// const img = new PIXI.Sprite.from("images/dupro/dupro-head.jpg");
// img.width = IMG_WIDTH,
// img.height = IMG_HEIGHT,
// app.stage.addChild(img),
// depthMap = new PIXI.Sprite.from("images/dupro/dupro-depth.jpg"),
// depthMap.width = IMG_WIDTH,
// depthMap.height = IMG_HEIGHT,
// app.stage.addChild(depthMap),
// displacementFilter = new PIXI.filters.DisplacementFilter(depthMap),
// app.stage.filters = [displacementFilter],
// displacementFilter.scale.x = 0,
// displacementFilter.scale.y = 0,
// container.addEventListener("mousemove", (e => {
//     displacementFilter.scale.x = .1 * (IMG_WIDTH / 2 - e.clientX) / 20,
//     displacementFilter.scale.y = .1 * (IMG_HEIGHT / 2 - e.clientY) / 20
// }));

// const container =  document.querySelector(".header-boby");
// const IMG_WIDTH = window.screen.width;
// const IMG_HEIGHT = container.offsetHeight;







